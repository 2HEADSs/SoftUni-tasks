function solve(input) {
  let data = input.toLowerCase().split(" ");
  console.log(data);
  for (let key of data) {
    let count = 0;

    for(let el of)
  }
}
solve("Java C# Php PHP Java PhP 3 C# 3 1 5 C#");
