function solve(input) {
    let n = Number(input[0]);
    let leftSum = 0;
    let rightSum = 0;

    for(let i =1;i<=n; i++){
        leftSum += Number(input[i]);
    }
}
solve(["2", "10", "90", "60", "40"]);
solve(["2", "90", "9", "50", "50"]);
