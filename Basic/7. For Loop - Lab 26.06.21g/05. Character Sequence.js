function solve(input) {
  let text = input[0];

  for (let i = 0; i < text.length; i++) {
    let ch = text[i];
    console.log(ch);
  }
}
solve(["softuni"]);
console.log("-----------");
solve(["ice cream"]);
